{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // Tests run in Node — allow Node types
    "types": ["vitest/globals"],
    "noEmit": true
  },
  "include": [
    "src/**/*.ts",
    "types/**/*.d.ts",
    "tests/**/*.ts"
  ]
}
